fun fib(n) {
  // Base cases for Fibonacci sequence: fib(0) = 0, fib(1) = 1
  if (n <= 1) {
    return n;
  }
  // Recursive step: fib(n) = fib(n-1) + fib(n-2)
  return fib(n - 1) + fib(n - 2);
}


var result = fib(32);


print result;
